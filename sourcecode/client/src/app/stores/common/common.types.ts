export interface ICommon{
    result:boolean,
    hasError:boolean,
    error:string,
    country:string,
    exchange:string,
    exchangeData:boolean,
    script:string,
    scriptData:boolean,
    instrument:string,
    instrumentData: boolean,
    segment:string,
    segmentData: boolean,
    exchange_id: number;
    script_id: number; 
}
